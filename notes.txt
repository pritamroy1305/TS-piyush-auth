What am i Doing 

pnpm init 
tsc --init
src folder --> give path for root and output
npx gitignore Node


change scripts 
"scripts": {
  "start": "node dist/index",
  "build": "tsc -p .",
  "dev": "tsc-watch --onSuccess \"node dist/index\""
}

install tsc-watch as dev dependency

#npm Express and its types

SETup Server by code 

install handlers EXPRESS  (A handler is simply a function that runs when a specific route is hit.
It receives the request and sends a response.)

create app folder here my business logic goes

now setup the express inside app / indix file 
then tell serve to handle my routes

#Setup Docker 
i need a pg container --> Make docker compose file 
setup docker locally i docker.composre.yml file

 ports:
      - "5432:5432"  port mapping 

volumes: 
      - pgdata:/var/lib/postgresql/data
      volume mapping that creates the memeory in local machine permanent 

      docker ps
      docker compose up -D 

#Connection to DB / Drizzle 

follow this link--  https://orm.drizzle.team/docs/get-started/postgresql-new

db object is you Connection

Now make tables
table creted

DB migration schema --> Db

setup studio "studio": "drizzle-kit studio"

now do db migration  --> npx drizzle-kit push

dnt directly push see firthst the sql file then push 







